import React, { useState, useEffect } from "react";

const useHttp = (taskText) => {
    const [data, setData] = useState([])
    const [error, setError] = useState(null)


};

export default useHttp;